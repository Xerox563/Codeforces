#include <bits/stdc++.h>
using namespace std;
int main()
{
    long long tc;
    cin >> tc;
    while (tc--)
    {
        int n;
        cin >> n;
        string str;

        int k;
        cin >> k;
        string temp = "";
        for (int i = 0; i < k; i++)
        {
            char ch;
            temp += ch;
        }
        // Solution Goes Here
        for (int i = 0;)
    }
    return 0;
}