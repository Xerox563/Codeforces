#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, q, k;
    cin >> n >> q >> k;
    int arr[n + 2]{};
    for (int i = 1; i <= n; i++)
    {
        cin >> arr[i];
    }
    while (q--)
    {
        int l, r, val;
        cin >> l >> r >> val;
        arr[l] += val;
        arr[r + 1] -= val;
    }
    while (k--)
    {
        for (int j = 0; j < n; j++)
        {
            if (Arr[i] % mod)
                == x
                {
                    if (a[i])% x == 0) [
                x++;
            ]
                }
        }
    }

    return 0;
}