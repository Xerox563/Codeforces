#include <bits/stdc++.h>
using namespace std;
long long solve(int n, int m, int k)
{
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    long long t;
    cin >> t;
    while (t--)
    {
        int n, m, k;
        cin >> n >> m >> k;

        cout << solve(n, m, k) << endl;
    }
    return 0;
}