#include <bits/stdc++.h>
using namespace std;
void solve(string str, int n)
{
    for(int i=0;i<n;i++) {
        if(str[i]== '0' && str[i+1] == '1' ) {
            
        }
    }
}

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        string str;
        cin >> str;
        solve(str, n);
    }
    return 0;
}