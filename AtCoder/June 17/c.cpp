#include <bits/stdc++.h>
using namespace std;

void Solve()
{
    int n, m;
    cin >> n >> m;
    while (n--)
    {
        string str;
        cin >> str;
        
    }
}

int main()
{

    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);

    long long tc;
    // cin >> t;
    tc = 1;

    while (tc--)
    {
        Solve();
    }
    return 0;
}
