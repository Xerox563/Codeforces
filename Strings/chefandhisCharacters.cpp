#include <bits/stdc++.h>
using namespace std;
unordered_map<char, int> mp;
void solve(string &str)
{
    
}
int main()
{
    int T;
    cin >> T;
    while (T--)
    {
        string str;
        cin >> str;
        solve(str);
       
        mp.insert({'c', 1});
        mp.insert({'h', 1});
        mp.insert({'e', 1});
        mp.insert({'f', 1});
    }
    return 0;
}